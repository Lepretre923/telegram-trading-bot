[project]
name = "trading-bot"
version = "0.1.0"
description = "Bot de trading crypto complet avec alertes, analyses et scan de marché"
requires-python = ">=3.11"

dependencies = [
    "flask>=3.1.3",
    "python-telegram-bot>=22.6",
    "requests>=2.32.5",
    "schedule>=1.2.2",
    "pandas>=3.0.1",
    "ta>=0.11.0"
]
